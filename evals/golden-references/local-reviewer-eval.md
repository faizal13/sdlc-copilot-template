# Golden References — @local-reviewer

Use these input/output pairs to evaluate @local-reviewer quality.

---

## Reference 1: Clean Code — Ready to Commit

### INPUT
Code generated by @local-rakbank-dev-agent for a simple CRUD story.
- `mvn compile` passes
- `mvn test` passes (12/12 tests)
- `mvn checkstyle:check` passes
- `mvn verify` passes
- All ACs have corresponding test methods
- BigDecimal used for monetary fields
- Persona isolation enforced in service layer

### EXPECTED OUTPUT — Key Quality Markers
- [ ] Mechanical verification section FIRST (before subjective review)
- [ ] Compile: PASS, Tests: PASS (12/12), Checkstyle: PASS, Verify: PASS
- [ ] 0 CRITICAL issues
- [ ] 0-3 WARNINGS (minor — missing Javadoc on internal methods, etc.)
- [ ] 1-3 SUGGESTIONS (nice-to-haves — stream API opportunity, etc.)
- [ ] AC COVERAGE CHECK: all ACs mapped to test methods with checkmarks
- [ ] VERDICT: "READY TO COMMIT"
- [ ] REVIEW-RESULT-JSON block with structured data (Gap 4 enhancement)

### SCORING GUIDE
| Dimension | What to check |
|-----------|---------------|
| Completeness | All sections present, mechanical checks run, AC coverage checked |
| Precision | Specific file:line references for each finding (not vague "the code looks fine") |
| Standards | Banking-specific checks performed (BigDecimal, persona isolation, state machine) |
| Actionability | Developer knows exactly what to do next (commit commands provided) |

---

## Reference 2: Code with Critical Issues — Blocked

### INPUT
Code with issues:
- `mvn test` fails (2 failures)
- `double` used for `amount` field (should be BigDecimal)
- Missing persona isolation check in service layer
- AC3 has no corresponding test method

### EXPECTED OUTPUT — Key Quality Markers
- [ ] Mechanical verification: Tests FAIL (2 failures listed by name)
- [ ] CRITICAL issues (3 minimum):
  - `double amount` → must be `BigDecimal` with file:line reference
  - Missing persona isolation → specific service method identified
  - Missing AC3 test → specific test file and expected method name
- [ ] Each critical has: Issue, Fix (specific instruction), Why (banking/security reason)
- [ ] AC COVERAGE CHECK shows AC3 as "missing"
- [ ] VERDICT: "BLOCKED — 3 critical issues must be resolved first"
- [ ] "WHAT TO DO NEXT" references @local-reviewer re-run after fixes

### ANTI-PATTERNS (should score 0.0 on Precision if present)
- "The code generally looks good" (vague — no specific evidence)
- "READY TO COMMIT" despite test failures (incorrect verdict)
- Missing file:line references on findings
- Reporting compilation errors as "warnings" instead of "critical"

---

## Reference 3: Code with Warnings Only — Ready with Notes

### INPUT
Code that compiles and tests pass, but:
- 2 methods missing Javadoc
- 1 hardcoded timeout value (should be in application.yml)
- 1 unused import

### EXPECTED OUTPUT — Key Quality Markers
- [ ] 0 CRITICAL issues
- [ ] 3 WARNINGS with specific file:line for each
- [ ] Each warning has: Issue + Fix (concrete instruction)
- [ ] AC COVERAGE: all green
- [ ] VERDICT: "READY TO COMMIT" (warnings don't block)
- [ ] Suggestions about extracting the timeout to config
